name: Deploy to Oracle

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Oracle 서버에 SSH로 배포
        uses: appleboy/ssh-action@v1
        env:
          REPO_URL: ${{ github.server_url }}/${{ github.repository }}.git
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          envs: REPO_URL
          command_timeout: "30m"
          script: |
            set -e
            cd /home/ubuntu/skinforge

            if [ ! -d .git ]; then
              git init
              git remote add origin "$REPO_URL"
            fi

            git fetch origin main
            git reset --hard origin/main

            echo "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" > .env
            echo "APP_USER=${{ secrets.APP_USER }}" >> .env
            echo "APP_PW=${{ secrets.APP_PW }}" >> .env

            sudo cp nginx-skinforge.conf /etc/nginx/sites-available/skinforge
            sudo ln -sf /etc/nginx/sites-available/skinforge /etc/nginx/sites-enabled/skinforge
            sudo nginx -t && sudo systemctl reload nginx

            set +e
            docker compose down --remove-orphans 2>/dev/null
            docker stop skinforge 2>/dev/null
            docker rm -f skinforge 2>/dev/null
            docker ps -aq --filter name=skinforge | xargs -r docker rm -f 2>/dev/null
            docker network rm skinforge_default 2>/dev/null
            set -e

            docker compose up -d --build --force-recreate > /dev/null 2>&1
            echo "compose up done"

      - name: 헬스체크
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          command_timeout: "5m"
          script: |
            sleep 15
            echo "=== container status ==="
            docker ps -a --filter name=skinforge --format "{{.Names}} {{.Status}}"
            echo "=== app logs ==="
            docker logs skinforge --tail 50 2>&1
            echo "=== port ==="
            ss -tlnp | grep 7860 || echo "7860 not bound"
            echo "=== nginx test ==="
            curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:7860/ || echo "curl failed"
